torch
torchvision
# LTX-2.5 needs a Gemma-4 text encoder and the ltx2 pipeline, neither of which
# is in any released transformers/diffusers. Both live only on git main, and
# without them the worker silently falls back to an older model.
git+https://github.com/huggingface/transformers
git+https://github.com/huggingface/diffusers
accelerate
sentencepiece
imageio[ffmpeg]
fastapi
uvicorn
pydantic>=2
tiktoken
protobuf
soundfile
numpy
